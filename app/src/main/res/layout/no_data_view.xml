<?xml version="1.0" encoding="utf-8"?>
<merge xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:gravity="center"
    android:orientation="vertical"
    android:padding="@dimen/empty_state_padding"
    tools:parentTag="android.widget.LinearLayout">

    <com.google.android.material.imageview.ShapeableImageView
        android:id="@+id/noDataPicto"
        android:layout_width="@dimen/empty_state_picto_size"
        android:layout_height="@dimen/empty_state_picto_size"
        android:scaleType="fitCenter"
        android:tint="@color/empty_state" />

    <TextView
        android:id="@+id/noDataMessage"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:fontFamily="sans-serif-thin"
        android:gravity="center"
        android:textColor="@color/empty_state"
        android:textSize="30sp" />

</merge>